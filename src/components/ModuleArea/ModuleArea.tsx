import { Box } from "@radix-ui/themes";
import React from "react";

type Props = {};

const ModuleArea = (props: Props) => {
  return <Box minHeight="200px">ModuleArea</Box>;
};

export default ModuleArea;
